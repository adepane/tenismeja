<?php

use Core\Core;

class dashboardController extends Core
{
    public function getdashboard()
    {
        // $User = Auth::user();
        // $returnData = array();
    }

    public function getnotification()
    {
        // $User = Auth::user();
        // $getnotif = Notification::where('read',0)
        // ->where(function($x)use($User){
        // 	if ($User->permission > 1) {
        // 		$x->where('bidang_id',$User->permission);
        // 	}
        // })->count();
        // return $getnotif;
    }
}
